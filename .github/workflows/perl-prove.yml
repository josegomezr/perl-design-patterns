---
name: 'Tests'

on:
  pull_request:
  push:
    branches:
      - 'main'

jobs:
  prove:
    uses: josegomezr/perl-toolkit/.github/workflows/perl-prove.yml@4a2ed5e
    name: "Prove"
    with:
      prove-args: '--rc=.proverc.gha'
